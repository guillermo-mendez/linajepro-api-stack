#docker-compose V0.0.2'
version: "3.9"
services:
  database:
    image: "mariadb"
    container_name: linaje-pro-db
    restart: always
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=test
      - MYSQL_DATABASE=linajepro_master
      - TZ=America/Bogota
